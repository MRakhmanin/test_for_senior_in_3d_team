# Тестовое задание на позицию Senior Computer Vision Engineer в 3D-команду
 
Мы постарались сделать задание, которое максимально симулирует работу в нашей команде: когда проблема найдена, а не поставлена сверху, и её нужно проанализировать и реализовать решение, используя то, что у нас уже есть. А есть, как правило, сабсет наших данных, уже обученные модели и разметка, которая имеется, но часто не относится к данной проблеме напрямую.
 
Задание проверяет вашу креативность, способность решать нестандартные задачи и умение писать чистый и понятный код.
 
## Задание
 
### Нормализация ориентации головы на КТ головного мозга
 
**Контекст.** Часть КТ-исследований головного мозга поступает к нам с произвольной ориентацией головы: при укладке пациента в аппарате голова оказывается наклонённой или повёрнутой. В реконструированном 3D-объёме (volume) анатомические оси мозга в итоге не совпадают от исследования к исследованию.
 
**Проблема.** Наши системы (детекция и сегментация патологий, измерения, сопоставление исследований) рассчитывают на единую, каноническую укладку головы. Произвольная ориентация снижает их точность и воспроизводимость.
 
**Цель.** Реализовать автоматический препроцессинг-шаг, который приводит голову к единой канонической ориентации **в 3D**: выравнивает объём по всем трём осям вращения так, что любая входная ориентация всегда отображается в одну и ту же стандартную.
 
**Критерии выравнивания**
 
- Срединно-сагиттальная плоскость вертикальна и симметрична - нет наклона к плечу.
- Лицо направлено строго вперёд - нет поворота влево/вправо.
- Наклон вперёд/назад отсутствует.

### Картинки-визуализации
 
#### Три независимые оси вращения головы
 
![Три независимые оси вращения головы](./assets/three_rotation_axes_head.png)
 
#### Визуализация выравнивания в 3D
 
![Визуализация поворота в 3D](./assets/orientation_normalization_goal_total.jfif)
 
## Что нужно сделать
 
- Описать архитектуру / дизайн решения проблемы.
- Придумать способ оценки качества решения и реализовать его.
- Сделать API сервер, который можно поднять через docker / docker-compose, загрузить в него КТ-скан мозга по HTTP и получить на выходе КТ-скан, выровненный вашим алгоритмом + афинную матрицу поворота, по которой была сделана трансформация. Используйте любой веб-фреймворк.
- Написать инструкцию и документацию к репозиторию. Насколько подробную - решать вам, но мы должны суметь запустить ваше решение у себя.

## Данные
 
Мы подготовили датасет из 100 КТ-сканов головного мозга (взят из датасета [CQ500](https://www.kaggle.com/datasets/crawford/qureai-headct)). 

Также сгенерировали маски анатомических структур мозга с помощью [TotalSegmentator](https://github.com/wasserth/totalsegmentator) (опенсорсная модель).
 
[Ссылка на скачивание](https://nextcloud.celsus.dev/s/mR2JGoTKjCGYbes)
 
### Критерии повёрнутости головы
 
В качестве примеров мы предлагаем вам посмотреть:
 
- На 2 примера идеальных КТ-сканов, поворот которых не требуется. (`volumes/CQ500CT06.nii.gz`, `volumes/CQ500CT243.nii.gz`). КТ-скан `volumes/CQ500CT243.nii.gz` с патологией, но при этом выравнен правильно.
- На 1 пример КТ-скана, ориентация которого требует поворота. (`volumes/CQ500CT48.nii.gz`)
- Допускается поворот головы на +- 5 градусов от идеального положения по каждой из осей в качестве погрешности.

### Структура датасета
 
```
dataset_for_test_to_3d_team
    |___volumes
        |___{fname_1}.nii.gz  # NIFTI формат КТ-скана головного мозга
        |___{fname_n}.nii.gz  # NIFTI формат КТ-скана головного мозга
    |___masks
        |___{fname_1}
            |____{totalsegmentator_task}  # имя задачи из списка задач TotalSegmentator
                |___{roi_1}.nii.gz  # бинарная маска в NIFTI формате
                |___{roi_n}.nii.gz  # бинарная маска в NIFTI формате
        |___{fname_n}
```
 
где `roi_n` — имя класса, предсказываемого в TotalSegmentator, `totalsegmentator_task` - название задачи в тотал сегментаторе, просто идентификатор используемой модели.
 
### Визуализация
 
Для визуализации данных можно использовать [ITK-SNAP](https://www.itksnap.org/pmwiki/pmwiki.php): в нём удобно смотреть КТ-скан в формате NIFTI и маски к нему.
 
Для отображения откройте .nii.gz фаил из папки volumes (либо перенесите его через drag-n-drop). После можно так же через drag-n-drop перенести на него любую из масок.
 
**Важно!** Во вкладке `Tools` -> `Image Contrast` -> `Contrast Adjustment` -> `Contrast` выставите `Level = 40`, `Window = 90` для лучшей визуализации.
 
![Визуализация в ITK-SNAP](./assets/itk_snap_ex.png)
 
### Пример чтения на Python
 
```python
import SimpleITK as sitk
import numpy as np
 
def read_nifti(path):
    """Читает .nii.gz через SimpleITK.
 
    Возвращает:
    np.array с шейпом (D, W, H)
    словарь с частью метаданных
    """
    img = sitk.ReadImage(path)
    meta = {
        "spacing":   img.GetSpacing(),     # (x, y, z)
        "origin":    img.GetOrigin(),
        "direction": img.GetDirection(),
        "size":      img.GetSize(),        # (x, y, z)
    }
    return sitk.GetArrayFromImage(img), meta   # array уже в порядке (z, y, x)
```
 
## Критерии решения
 
1. Поворот обязательно осуществляется в 3D-пространстве; решения только для одной 2D-плоскости не принимаются.
2. Есть способ оценить качество решения.
3. Есть способ запуска вашего решения для любого КТ-скана в формате NIFTI, которое вернёт повёрнутый КТ-скан + афинная матрица преобразования. Решение упаковано в докер.
4. Допускается погрешность выравнивания +- 5 градусов по каждой из осей.
5. Нужно реализовать **минимум один простой baseline** и **минимум одну обучаемую модель**

## Формат решения
 
1. Сделать fork репозитория и скинуть нам ссылку на него.
2. Инструкцию по запуску описать в `README.md`.
3. Инструкцию по воспроизведению результатов обучения / тестирования моделей также описать в `README.md`.
4. Дополнительную документацию к коду и решению задачи можно вынести в `docs/*.md`.
5. Написать отдельный фаил отчёт.

В отчёте нужно описать:
 
- Как вы поняли задачу
- Какую постановку задачи выбрали
- Как получили target из разметки
- Какие подходы попробовали
- Какой baseline реализовали
- Какую модель обучили
- Какие loss/метрики использовали
- Результаты на val
- Сравнение baseline и модели

## Пожелания
 
1. Можно использовать любые инструменты. В наших командах мы активно используем Claude Code, поэтому так же не против использоватия GenAI инструментов; в этом случае оставьте в репозитории `CLAUDE.md` / `AGENTS.md` и остальные файлы ИИ-ассистентов.
2. Если хотите, можете доразметить любое количество данных или использовать любые опенсорсные инструменты, включая предобученные модели и датасеты. Главное - указать, откуда они взяты.
3. Можете так же выполнить разметку части данных самостоятельно руками, если в этом есть необходимость.

## Немного мед терминологии

**Как поворот головы оценивает врач**

Врач оценивает ориентацию головы по трём проекциям:

- **Аксиальная** — серп мозга образует строгую вертикальную линию по центру на виде сверху. Если серп мозга деформирован (например, изогнут дугой, смещен опухолью или гематомой), в качестве ориентира для выравнивания аксиальной оси используется воображаемая прямая линия, соединяющая две крайние, фиксированные точки серпа мозга.
- **Корональная** — по наклону головы к левому или правому плечу: парные анатомические структуры справа и слева должны лежать строго на одной горизонтальной линии.
- **Сагиттальная** — по орбитомеатальной линии (для упрощения — линии между центром глаза и центром наружного слухового прохода должна быть горизонтальна).

## Контакты
 
По всем вопросам — в Telegram: [@PotekhinRoman](https://t.me/PotekhinRoman)